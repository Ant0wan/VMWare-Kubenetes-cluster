apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: "<name>"
      protocol: layer2
      addresses:
      - {{ addresses[0] }}-{{ addresses[1] }}
