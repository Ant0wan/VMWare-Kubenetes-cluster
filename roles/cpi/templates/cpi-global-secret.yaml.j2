apiVersion: v1
kind: Secret
metadata:
  name: cpi-global-secret
  namespace: kube-system
stringData:
  {{ vcenter_ip }}.username: "{{ vcenter_username }}"
  {{ vcenter_ip }}.password: "{{ vcenter_password }}"
